# AI Shopping Assistant - Architecture Diagram

## Complete System Overview (Phases 1-3)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                            USER INTERFACE                                │
│                         (http://localhost:3000)                          │
└─────────────────────────────────────────────────────────────────────────┘
                                    │
                    ┌───────────────┴───────────────┐
                    │                               │
         ┌──────────▼──────────┐       ┌───────────▼──────────┐
         │   AI Chat Panel     │       │   Product Pages      │
         │   - Message input   │       │   - Browse products  │
         │   - Chat history    │       │   - Customize items  │
         │   - Action cards    │       │   - Add to cart btn  │
         └──────────┬──────────┘       └───────────┬──────────┘
                    │                               │
                    │         ┌─────────────────────┤
                    │         │                     │
         ┌──────────▼─────────▼──────┐   ┌─────────▼─────────┐
         │   AI Assistant Context    │   │   Cart Context    │
         │   - sessionId             │   │   - items         │
         │   - messages              │   │   - itemCount     │
         │   - sendMessage()         │   │   - totalPrice    │
         └──────────┬────────────────┘   │   - addItem()     │
                    │                     │   - refreshCart() │
                    │                     └─────────┬─────────┘
                    │                               │
                    │         ┌─────────────────────┘
                    │         │
         ┌──────────▼─────────▼──────┐
         │    Cart Icon (Navbar)     │
         │    Shows: itemCount       │
         │    Badge updates on sync  │
         └───────────────────────────┘
                    │
         ┌──────────▼────────────────┐
         │    Frontend API Layer     │
         │    (web/client/services)  │
         │                           │
         │    - sendChatMessage()    │
         │    - addToCart()          │
         │    - getCart()            │
         │    - removeFromCart()     │
         │    - updateCartQuantity() │
         └──────────┬────────────────┘
                    │
                    │ HTTP Requests
                    │ (axios)
                    ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     BACKEND API SERVER                                   │
│                 Django REST Framework                                    │
│                 (http://localhost:8000)                                  │
└─────────────────────────────────────────────────────────────────────────┘
                    │
      ┌─────────────┼─────────────┐
      │             │             │
┌─────▼─────┐ ┌────▼────┐  ┌─────▼─────┐
│  AI Chat  │ │  Cart   │  │ Products  │
│ Endpoints │ │  CRUD   │  │   API     │
│           │ │  API    │  │           │
│ /chat/    │ │ /cart/  │  │ /api/...  │
└─────┬─────┘ └────┬────┘  └───────────┘
      │            │
      │            │
      ▼            ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                        AI AGENT SERVICE                                  │
│                      (LangChain + GPT-4)                                 │
│                                                                          │
│  System Prompt:                                                          │
│  "You are a shopping assistant. You CAN take action!                    │
│   When user says 'I want', use add_to_cart tool..."                     │
│                                                                          │
│  Available Tools:                                                        │
│  ┌────────────────┐  ┌──────────────┐  ┌────────────────┐             │
│  │ add_to_cart    │  │ view_cart    │  │ search_products│             │
│  │ - session_id   │  │ - session_id │  │ - query        │             │
│  │ - product_id   │  │              │  │                │             │
│  │ - quantity     │  └──────────────┘  └────────────────┘             │
│  │ - config       │                                                     │
│  └────────┬───────┘                                                     │
│           │                                                             │
│           │ Tool executes                                               │
│           ▼                                                             │
│  ┌──────────────────────────────────────────────────────┐             │
│  │         Redis Cart Service                           │             │
│  │         (cart_service.py)                            │             │
│  │                                                       │             │
│  │  Methods:                                            │             │
│  │  - add_item(session_id, product_id, ...)            │             │
│  │  - get_cart(session_id)                              │             │
│  │  - remove_item(session_id, item_id)                  │             │
│  │  - update_quantity(session_id, item_id, qty)         │             │
│  │  - clear_cart(session_id)                            │             │
│  └──────────────────┬───────────────────────────────────┘             │
│                     │                                                   │
└─────────────────────┼───────────────────────────────────────────────────┘
                      │
                      │ Redis commands
                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                          REDIS (DB 1)                                    │
│                      In-Memory Cart Storage                              │
│                     (7-day TTL auto-expiring)                            │
│                                                                          │
│  Key Structure:                                                          │
│  ┌────────────────────────────────────────────────────────────┐        │
│  │ cart:{session_id}                  (Hash)                  │        │
│  │   - updated_at: "2025-10-16T10:30:00"                      │        │
│  │   - customer_id: "123" (optional)                          │        │
│  │   - phone_number: "+256..." (optional)                     │        │
│  │   TTL: 604800 seconds (7 days)                             │        │
│  └────────────────────────────────────────────────────────────┘        │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────┐        │
│  │ cart:{session_id}:items            (Set)                   │        │
│  │   Members: ["123_hash", "456_hash", "789_hash"]            │        │
│  │   TTL: 604800 seconds (7 days)                             │        │
│  └────────────────────────────────────────────────────────────┘        │
│                                                                          │
│  ┌────────────────────────────────────────────────────────────┐        │
│  │ cart:{session_id}:item:{item_id}   (Hash)                  │        │
│  │   - product_id: "123"                                       │        │
│  │   - name: "Balloon Bouquet"                                │        │
│  │   - price: "60000"                                          │        │
│  │   - quantity: "2"                                           │        │
│  │   - configuration: "{...}"                                 │        │
│  │   - image_url: "/images/balloons.jpg"                      │        │
│  │   - category_id: "5"                                        │        │
│  │   TTL: 604800 seconds (7 days)                             │        │
│  └────────────────────────────────────────────────────────────┘        │
└─────────────────────────────────────────────────────────────────────────┘

```

## Data Flow: AI Adds Item to Cart

### Step-by-Step Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 1: User types message                                              │
└─────────────────────────────────────────────────────────────────────────┘
   User: "I want 2 balloon bouquets"
   Frontend: AI Assistant Context receives input

   ↓ HTTP POST /api/ai-assistant/chat/

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 2: Backend processes chat request                                  │
└─────────────────────────────────────────────────────────────────────────┘
   Django View: chat(request)
   - session_id: "session-abc123"
   - message: "I want 2 balloon bouquets"

   ↓ Call agent_service.chat()

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 3: AI Agent analyzes message                                       │
└─────────────────────────────────────────────────────────────────────────┘
   LangChain Agent:
   - Reads system prompt
   - Sees "WHEN TO ADD TO CART: User says 'I want'..."
   - Analyzes: "User wants to buy → I should use add_to_cart tool"
   - Searches products for "balloon bouquets"
   - Finds: Product ID 42, Price: 60000 UGX

   ↓ Execute add_to_cart tool

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 4: Tool execution (add_to_cart)                                    │
└─────────────────────────────────────────────────────────────────────────┘
   AddToCartTool._run(
     session_id="session-abc123",
     product_id=42,
     product_name="Balloon Bouquet",
     price=60000,
     quantity=2
   )

   ↓ Call cart_service.add_item()

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 5: Redis cart update                                               │
└─────────────────────────────────────────────────────────────────────────┘
   RedisCartService.add_item():

   1. Generate item_id = "42_" + hash(config)
   2. Check if item exists: SISMEMBER cart:session-abc123:items, "42_hash"
   3. Add to set: SADD cart:session-abc123:items "42_hash"
   4. Store item: HSET cart:session-abc123:item:42_hash
      {
        "product_id": "42",
        "name": "Balloon Bouquet",
        "price": "60000",
        "quantity": "2",
        "configuration": "{}",
        "image_url": "/images/balloons.jpg",
        "category_id": "5"
      }
   5. Update metadata: HSET cart:session-abc123 "updated_at" "2025-10-16..."
   6. Refresh TTL: EXPIRE cart:session-abc123 604800

   ↓ Return item details

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 6: Tool returns result                                             │
└─────────────────────────────────────────────────────────────────────────┘
   Tool result: "✅ Added 2x Balloon Bouquet to cart! (UGX 120,000)"

   ↓ AI Agent formats response

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 7: Agent creates response message                                  │
└─────────────────────────────────────────────────────────────────────────┘
   AI Response:
   {
     "role": "assistant",
     "content": "✅ Added 2x Balloon Bouquet to your cart! (UGX 120,000)\n\n
                 Your cart total is now UGX 120,000 with 2 items.\n\n
                 Ready to checkout?",
     "metadata": {
       "tools_used": ["add_to_cart"],
       "intermediate_steps": [...]
     }
   }

   ↓ Back to Django view

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 8: Backend detects tool usage and enriches response                │
└─────────────────────────────────────────────────────────────────────────┘
   Django View (chat):
   - Detects: "add_to_cart" in tools_used
   - Fetches cart: cart_service.get_cart("session-abc123")
   - Adds action metadata:
     {
       "action": {
         "type": "item_added",
         "cart_items": [
           {
             "item_id": "42_hash",
             "product_id": 42,
             "name": "Balloon Bouquet",
             "price": 60000,
             "quantity": 2,
             "line_total": 120000
           }
         ],
         "cart_total": 120000,
         "item_count": 2
       }
     }

   ↓ HTTP Response to frontend

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 9: Frontend receives response                                      │
└─────────────────────────────────────────────────────────────────────────┘
   AI Assistant Context:
   - Receives message with action metadata
   - Adds to messages state
   - Triggers re-render

   ↓ Render chat message

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 10: AI Chat Message renders                                        │
└─────────────────────────────────────────────────────────────────────────┘
   AIChatMessage component:
   - Displays AI text: "✅ Added 2x Balloon Bouquet..."
   - Detects action metadata
   - Renders AIActionCard component

   ↓ Action card mounts

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 11: Action card triggers cart refresh                              │
└─────────────────────────────────────────────────────────────────────────┘
   AIActionCard useEffect:
   useEffect(() => {
     if (action.type === 'item_added') {
       refreshCart()  // 🔄 Trigger sync
     }
   }, [action.type])

   ↓ Call cart context refreshCart()

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 12: Cart context fetches from backend                              │
└─────────────────────────────────────────────────────────────────────────┘
   Cart Context fetchCart():
   1. Call API: GET /api/ai-assistant/cart/session-abc123/
   2. Backend fetches from Redis
   3. Transforms response to CartItem format
   4. Updates state:
      - items: [{id: "42_hash", name: "Balloon Bouquet", ...}]
      - itemCount: 2
      - totalPrice: 120000

   ↓ State update triggers re-render

┌─────────────────────────────────────────────────────────────────────────┐
│ STEP 13: UI updates everywhere                                          │
└─────────────────────────────────────────────────────────────────────────┘

   ✅ Cart Badge (CartIcon component):
      - Reads itemCount from context
      - Displays badge with "2"
      - Badge becomes visible (was hidden when itemCount === 0)

   ✅ Action Card:
      - Shows green "Added to Cart!" card
      - Displays: "2x Balloon Bouquet (UGX 120,000)"
      - Shows buttons: "View Cart" and "Checkout Now"

   ✅ Cart Page (if open):
      - Displays all items from cart context
      - Shows correct quantities and prices

   🎉 COMPLETE!
```

## Session Management

```
┌────────────────────────────────────────────────────────────┐
│                    Session ID Flow                         │
└────────────────────────────────────────────────────────────┘

User opens app
     ↓
AI Assistant Context mounts
     ↓
Check localStorage for 'ai_session_id'
     ↓
     ├─ Found? Use existing session
     │         ↓
     │    Cart context fetches cart with this session_id
     │
     └─ Not found? Generate new session
               ↓
          session_id = "session-" + UUID()
               ↓
          Store in localStorage
               ↓
          Cart context fetches (empty cart)

┌────────────────────────────────────────────────────────────┐
│           Session ID Format & Usage                        │
└────────────────────────────────────────────────────────────┘

Web UI:
  Format: "session-abc123-def456-..."
  Stored: localStorage.getItem('ai_session_id')
  Usage: Links cart, chat history, user preferences

WhatsApp (Future):
  Format: "wa_256701618576" (phone number)
  Stored: Backend mapping table
  Usage: Same Redis cart service, different session format

Session persistence:
  - localStorage (web): Survives page reload, lost on clear
  - Redis cart: 7-day TTL, auto-expires
  - Chat history: Stored in backend (future)
```

## Cart Sync Mechanism

```
┌────────────────────────────────────────────────────────────┐
│              Cart Sync Triggers                            │
└────────────────────────────────────────────────────────────┘

Trigger 1: AI adds items
   AI Tool → Redis → Response with action metadata
   → Action card mounts → useEffect → refreshCart()

Trigger 2: User manually adds item
   Product page → addItem() → Backend API → Redis
   → fetchCart() → Update context state

Trigger 3: sessionId changes
   Context mounts with new sessionId
   → useEffect [sessionId] → fetchCart()

Trigger 4: Quantity update on cart page
   Cart page → updateQuantity() → Backend API → Redis
   → fetchCart() → Update context state

Trigger 5: Remove item
   Cart page → removeItem() → Backend API → Redis
   → fetchCart() → Update context state

All triggers lead to:
   Cart Context state update
        ↓
   itemCount, totalPrice recalculated
        ↓
   All consuming components re-render:
   - CartIcon badge
   - Cart page
   - Action cards
```

## Phase Completion Status

```
✅ Phase 1: Redis Cart & LangChain Tools
   - Redis service in Docker
   - Cart CRUD operations
   - 4 LangChain tools (add, view, remove, update)
   - Updated AI system prompt
   - 5 API endpoints

✅ Phase 2: Frontend Action Cards
   - AIActionCard component
   - Action metadata types
   - Cart API functions
   - Backend action detection

✅ Phase 3: Cart Context Sync
   - Removed localStorage
   - All operations use backend API
   - Auto-refresh on AI actions
   - Cart badge real-time updates

⏸️ Phase 4: Checkout Flow (Next)
   - Checkout initialization
   - Address collection
   - Shipping options
   - Order creation
   - Payment integration

⏸️ Phase 5: WhatsApp Integration
   - Meta Cloud API webhook
   - Message processing
   - Interactive messages
   - Order confirmations
```

## Redis Data Model

```
┌────────────────────────────────────────────────────────────┐
│              Redis Key Patterns (DB 1)                     │
└────────────────────────────────────────────────────────────┘

cart:{session_id}
├─ Type: Hash
├─ TTL: 7 days
├─ Fields:
│  ├─ updated_at: ISO timestamp
│  ├─ customer_id: (optional) Customer ID from DB
│  └─ phone_number: (optional) Phone for WhatsApp

cart:{session_id}:items
├─ Type: Set
├─ TTL: 7 days
└─ Members: Item IDs ["42_hash", "51_hash", ...]

cart:{session_id}:item:{item_id}
├─ Type: Hash
├─ TTL: 7 days
└─ Fields:
   ├─ product_id: Product ID from DB
   ├─ name: Product name
   ├─ price: Price per unit (string)
   ├─ quantity: Quantity (string)
   ├─ configuration: JSON string
   ├─ image_url: Image path
   └─ category_id: Category ID

Example:
cart:session-abc123 → {updated_at: "2025-10-16T10:30:00"}
cart:session-abc123:items → ["42_-9223372036854775808", "51_0"]
cart:session-abc123:item:42_-9223372036854775808 → {
  product_id: "42",
  name: "Balloon Bouquet",
  price: "60000",
  quantity: "2",
  ...
}
```

---

**Last Updated:** 2025-10-16
**Architecture Version:** 1.0 (Phases 1-3 Complete)
